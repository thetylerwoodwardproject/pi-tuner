# Pi Tuner v2 — Icecast connection (shared by all stations)
# These must match the <source-password> and listening address in your
# /etc/icecast2/icecast.xml.
#
# ─── user settings ─────────────────────────────────
HOST=localhost
PORT=8000
SOURCE_PASSWORD=CHANGEME
# ─── end user settings ─────────────────────────────
