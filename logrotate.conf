# Pi Tuner v2 local logs — size-based rotation.
# Installed to /etc/logrotate.d/pituner by install.sh.
/var/www/pituner/*.log {
    size 10M
    rotate 5
    copytruncate
    compress
    missingok
    notifempty
}
